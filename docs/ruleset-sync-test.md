# Ruleset sync test

This page exists to test the repository ruleset and GitBook Git Sync setup
for `main` (DOC-2042). It is not linked from any `SUMMARY.md`, so it doesn't
publish to the live site. Delete it after the test PR is verified.

See the [n8n docs](https://docs.n8n.io/) for the live site.
